package com.girisk.common.config;

/** girisk.config.v1 message kinds. */
public final class ScopeRiskConfigKinds {

    /** Config Center full release (legacy / global paramSet). */
    public static final String GLOBAL_RELEASE = "GLOBAL_RELEASE";

    /** Duty-layer override: OVERALL / SPORT / LEAGUE / MATCH. */
    public static final String SCOPE_OVERRIDE = "SCOPE_OVERRIDE";

    private ScopeRiskConfigKinds() {}
}
